// Scratch test used during a debugging session — intentionally emptied after the
// large-type-index investigation concluded. Safe to delete.
import { test, expect } from 'vitest'

test('placeholder - no scratch tests', () => {
	expect(true).toBe(true)
})
